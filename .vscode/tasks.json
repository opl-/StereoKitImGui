{
	// See https://go.microsoft.com/fwlink/?LinkId=733558
	// for the documentation about the tasks.json format
	"version": "2.0.0",
	"tasks": [
		{
			"label": "CMake (Debug)",
			"type": "shell",
			"group": "build",
			"command": "cmake --build . --parallel 10 --config Debug",
		},
		{
			"label": "CMake (Release)",
			"type": "shell",
			"group": "build",
			"command": "cmake --build . --parallel 10 --config Release",
		},
	]
}
